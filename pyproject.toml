[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/shell_delta"]

[tool.hatch.build]
exclude = [
  "/shelldelta_env",
  "/.git",
  "/.pytest_cache",
  "/**/__pycache__",
]

[project]
name = "ShellDelta"
version = "2026.0.1"
dependencies = [
  "PySide6",
  "PyOpenGL",
  "opencv-python",
  "numpy",
  "cel-expr-python",
  "psutil"
]
requires-python = ">=3.12"
authors = [
  {name = "shinonome-MiDUki", email = "shiinaayame3@gmail.com"}
]
maintainers = [
  {name = "shinonome-MiDUki", email = "shiinaayame3@gmail.com"}
]
description = "Shell Delta 2026"
readme = "README.md"
license = "Apache-2.0"
keywords = ["python", "shelldelta"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Programming Language :: Python"
]

[project.scripts]
delta = "shell_delta.__main__:main"

[project.urls]
Repository = "https://github.com/shinonome-MiDUki/Shell_DELTA"