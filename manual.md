# Shell Delta 取扱説明書

**Shell Delta** は、連番画像（シーケンス画像）をビデオと同期させながら編集・管理し、最終的にビデオファイルとして出力するための専門ツールです。

---

## 📖 目次

- [入門編：まずはここから](#入門編まずはここから)
  - [Shell Deltaとは？](#shell-deltaとは)
  - [必要なもの](#必要なもの)
  - [起動方法](#起動方法)
- [実践編：基本的な使い方](#実践編基本的な使い方)
  - [画面構成](#画面構成)
  - [シーケンス画像を読み込む](#1-シーケンス画像を読み込む)
  - [参照ビデオを設定する](#2-参照ビデオを設定する)
  - [フレームをナビゲートする](#3-フレームをナビゲートする)
  - [フレームマッピング（最重要）](#4-フレームマッピング最重要)
  - [ビデオをレンダリング（出力）する](#5-ビデオをレンダリング出力する)
  - [プロジェクトを保存・読み込み](#6-プロジェクトを保存・読み込み)
- [応用編：高度な機能](#応用編高度な機能)
  - [式エディタを使ったバッチ処理](#式エディタを使ったバッチ処理)
  - [TCL式エディタ](#tcl式エディタ)
  - [CEL式エディタ](#cel式エディタ)
  - [キーボードショートカット](#キーボードショートカット)
  - [メモリ管理](#メモリ管理)
- [サポート](#サポート)
  - [よくある質問](#よくある質問)
  - [トラブルシューティング](#トラブルシューティング)

---

# 入門編：まずはここから

## Shell Deltaとは？

### 🎬 具体例で理解しよう

あなたが 100 枚の連番画像（`frame_001.png`, `frame_002.png` ... `frame_100.png`）を持っていて、参考用のビデオ（`reference.mp4`）があるとします。

**こんなことが出来ます：**

```
frame_001.png → 別のフレーム画像を表示
frame_002.png → さらに別のフレーム画像を表示
frame_003.png → ...
```

このように、元の順番を変更して**新しい動画を作る**のがShell Deltaです。

### 🔍 主な特徴

| 機能 | 説明 |
|------|------|
| **画像の管理** | 連番画像ファイルを読み込んで表示・管理 |
| **ビデオ同期** | 参照ビデオと連番画像を並べて表示 |
| **フレーム再割り当て** | 「このフレームは別のフレームを使う」と指定 |
| **自動処理** | TCL/CEL式で大量のフレームを一括変更 |
| **ビデオ出力** | MP4など様々な形式でビデオを出力 |

---

## 必要なもの

### ✅ ファイル

1. **連番画像** - `frame_001.png`, `frame_002.png` など
   - PNG形式推奨
   - 同じディレクトリに全て入っていること
   - ファイル名に連続した数字を含むこと

2. **参照ビデオ**（あると便利）
   - MP4形式推奨
   - 同期用なので、必須ではありません

### 💻 環境

- Python 3.8 以上
- PySide6（PyQtに似たGUIライブラリ）
- OpenCV（cv2）
- OpenGL対応のグラフィックスカード

---

## 起動方法

ターミナル/コマンドプロンプトで以下を実行：

```bash
python main_win.py
```

Shell Deltaのメインウィンドウが開きます。

---

# 実践編：基本的な使い方

## 画面構成

```
┌─────────────────────────────────────────────────────────────┐
│  [Read Sequence] [Read Ref] [Save] [Open]  (ファイル操作)  │
│  Image Index: 42  Working Sequence: /path/to/folder/#####   │
├──────────────────┬──────────────────────────────────────────┤
│                  │                                          │
│   メイン画像     │  参照ビデオ (上)                         │
│  （現在のフレ）   │                                          │
│                  │  参照画像 (下)                           │
│                  │                                          │
├──────────────────┴──────────────────────────────────────────┤
│  [◀ Previous]  [ 42 ]  [Next ▶]  (フレーム操作)            │
├─────────────────────────────────────────────────────────────┤
│  [Play] [Pause] [Back] [Release Buff] [Render]  FPS: 24    │
├─────────────────────────────────────────────────────────────┤
│  [TCL ▼] [Run Expression] [Edit Expression] 0 ~ 100        │
│  (式エディタ - 応用機能)                                    │
└─────────────────────────────────────────────────────────────┘
```

### 🖱 各エリアの説明

| エリア | 役割 |
|--------|------|
| **上部（ファイル操作）** | シーケンス/ビデオ/プロジェクトを読み込み・保存 |
| **中央（画像表示）** | メイン画像と参照ビデオ/画像を並べて表示 |
| **下部（フレーム操作）** | 前後のフレームに移動、フレーム番号を入力 |
| **再生コントロール** | 再生・一時停止・バッファ管理・出力 |
| **式エディタ** | 複数フレームの一括変更（応用編で説明） |

---

## 1. シーケンス画像を読み込む

### ステップ 1: [Read Sequence] ボタンをクリック

ファイルダイアログが開きます。

### ステップ 2: シーケンスの最初の画像を選択

```
例: frame_001.png を選択
```

Shell Delta は自動的に以下を認識します：

- ✅ ファイル名に含まれる数字（`001`）→ フレーム番号
- ✅ その数字の桁数（`3桁`）→ ファイル命名ルール
- ✅ フォルダ内の他の連番ファイル（`frame_002.png` など）

### ステップ 3: 確認

画面に以下が表示されます：

```
Working Sequence: /path/to/folder/frame_###.png
Image Index: 1
現在のフレーム: 1
```

**これで準備完了！** 🎉

---

## 2. 参照ビデオを設定する

参照ビデオを横に並べて表示すると、フレーム調整がやりやすくなります。

### ステップ 1: [Read Ref] ボタンをクリック

### ステップ 2: ビデオファイル（MP4など）を選択

画面の右上に参照ビデオが表示されます。

### ステップ 3: ビデオの開始位置を指定（オプション）

参照ビデオの画面を**右クリック**します：

```
┌─────────────────────┐
│ Mark as Start      │ ← ここをクリック（現在位置を開始点に）
│ Reset Starting Point │ ← ここをクリック（リセット）
└─────────────────────┘
```

---

## 3. フレームをナビゲートする

### 方法 1: ボタンで移動

```
[◀ Previous]  → 1フレーム戻る
[Next ▶]      → 1フレーム進む
```

### 方法 2: フレーム番号を直接入力

```
現在のフレーム表示: [ 42 ]
                    ↑
                ここをクリックして数字を入力
                例: 85 と入力 → Enter キーを押す
```

### 方法 3: キーボードショートカット（応用編参照）

```
→ キー   次のフレーム
← キー   前のフレーム
Shift+→  10フレーム進む
Shift+←  10フレーム戻る
```

---

## 4. フレームマッピング（最重要）

### 💡 フレームマッピングとは？

**「このフレームには、別のフレーム画像を使う」と指定する操作です。**

#### 具体例

```
元のシーケンス:  frame_001.png → frame_002.png → frame_003.png
                     ↓              ↓              ↓
マッピング後:   frame_010.png → frame_015.png → frame_020.png
```

このように、フレーム順序を自由に変更できます。

### 🎯 実際にやってみよう

#### ステップ 1: フレームを移動

`[Previous]` `[Next]` または キーボードの `←→` で目的のフレームに移動します。

```
例: フレーム 5 に移動したい
→ 現在のフレーム表示を [5] に変更
```

#### ステップ 2: 数字キーを押す

**シーケンス画像を「使うフレーム番号」を入力します。**

```
例: フレーム5 で、frame_10.png を使いたい場合

1 → 0 と順に押す
↓
画面左上の "Image Index" に "10" と表示される
```

#### ステップ 3: Enter キーを押す

```
↓
マッピング完了！
画面のメイン画像が frame_010.png に変わります
```

#### 📊 マッピング結果

```
フレーム5  →  frame_010.png を使用
```

### ⚠️ 重要なポイント

| ポイント | 説明 |
|----------|------|
| **数字キー** | `0～9` を使用して 1 桁ずつ入力 |
| **Delete キー** | 入力中の数字をクリア |
| **Enter キー** | マッピングを確定 |
| **複数フレーム** | 1 フレームずつ操作、または式エディタで一括変更 |

### 🔄 フレームマッピングの見方

画面左上の表示に注目：

```
Image Index: 42
    ↑
    実際に表示されているフレーム画像の番号
    これが目的の番号になっていればマッピング成功！
```

---

## 5. ビデオをレンダリング（出力）する

### 🎬 レンダリングとは？

マッピング済みのシーケンス画像を、ビデオファイル（MP4など）に変換すること。

### ステップ 1: [Render] ボタンをクリック

レンダリングダイアログが開きます。

### ステップ 2: 出力設定

```
┌─────────────────────────────────────┐
│ Codec:      [H.264      ▼]         │ ← 圧縮形式
│ Container:  [.mp4       ▼]         │ ← ファイル形式
│ FPS:        [24         ]          │ ← 1秒あたりのフレーム数
│ Size:       [1920  X  1080]        │ ← 解像度（ピクセル）
│ Export Range: [0  ~  100]          │ ← 出力する範囲
│ Video Name: [my_video   ]          │ ← ファイル名
│ Directory:  [/path/to/dir] [Browse]│ ← 保存先フォルダ
│            [Render]                │ ← 実行
└─────────────────────────────────────┘
```

### 📝 各項目の説明

| 項目 | デフォルト | 説明 |
|------|-----------|------|
| **Codec** | H.264 | 動画の圧縮方式。H.264推奨（互換性が高い） |
| **Container** | .mp4 | ファイル形式。MP4推奨 |
| **FPS** | 24 | フレームレート。多くの場合24 or 30 |
| **Size** | 1920×1080 | 動画の解像度。大きいほど高品質（時間がかかる） |
| **Export Range** | 0～100 | 出力するフレーム範囲 |
| **Video Name** | （空） | ファイル名（拡張子は不要） |
| **Directory** | （空） | 保存先フォルダ |

### ⚙️ 設定例

```
【高品質・時間がかかる】
Codec:    H.264
FPS:      30
Size:     3840×2160 (4K)

【標準品質・バランス型】
Codec:    H.264
FPS:      24
Size:     1920×1080 (Full HD)

【低品質・高速】
Codec:    H.264
FPS:      24
Size:     1280×720 (HD)
```

### ステップ 3: 実行

1. **設定を確認**
2. **[Browse] でフォルダを選択** してファイルの保存先を決める
3. **ビデオ名を入力** 例: `my_animation`
4. **[Render] をクリック**

処理が始まります。完了すると "Rendered" と表示されます。

---

## 6. プロジェクトを保存・読み込み

### 💾 プロジェクトとは？

作成したマッピング設定を保存して、後で復元できるファイル形式です。

```
*.sdproj ファイル = 
  シーケンス情報 + フレームマッピング + 参照ビデオ情報
```

### 保存する

**[Save] ボタンをクリック**

```
初回: ファイルダイアログが開く → 保存先を選択 → ファイル名入力
2回目以降: 前回の場所に自動保存
```

### 読み込む

**[Open] ボタンをクリック**

```
ファイルダイアログが開く → *.sdproj ファイルを選択
↓
以前の作業が復元される
```

---

# 応用編：高度な機能

## 式エディタを使ったバッチ処理

複数のフレームのマッピングを**一括で自動変更**できる機能です。

```
例: フレーム1～100を「逆順で再生する」という処理
  フレーム1  → frame_100.png
  フレーム2  → frame_99.png
  フレーム3  → frame_98.png
  ...
```

### 式エディタの種類

| 種類 | 難易度 | 用途 |
|------|--------|------|
| **TCL** | ★★★ | より自由な処理、関数を定義できる |
| **CEL** | ★★ | 簡単な計算式、プリセットあり |

---

## TCL式エディタ

### TCLとは？

**Tool Command Language** - スクリプト言語。複雑な処理に向いています。

### 使い方

#### ステップ 1: 式エディタを選択

```
画面下部のドロップダウン: [TCL ▼]
```

### ステップ 2: [Edit Expression] をクリック

エディタウィンドウが開きます。

```
┌─────────────────────────────────┐
│  カスタム式を入力              │
│                                 │
│ （以前の式が表示される場合）    │
│  [Load TCL] [Save]              │
└─────────────────────────────────┘
```

### ステップ 3: TCL式を記述

#### 📚 利用可能な変数

| 変数 | 説明 | 例 |
|------|------|-----|
| `$frame` | 現在のフレーム番号 | 1, 2, 3... |
| `$seq_count` | シーケンスの総フレーム数 | 100 |
| `$loop_count` | 処理するフレーム範囲の数 | 50 |
| `$cframe` | 現在のマッピング済みフレーム | - |

#### 🎯 基本的な例

```tcl
# 例1: ループ再生
# フレームを seq_count の数だけ繰り返す
expr { (($frame - 1) % $seq_count) + 1 }

# 例2: 逆順再生
# フレームを逆順で再生
expr { $seq_count - (($frame - 1) % $seq_count) }

# 例3: フレームをスキップ
# 2フレームごとに再生
expr { (($frame - 1) / 2) % $seq_count + 1 }
```

### ステップ 4: フレーム範囲を指定

画面下部：

```
loop over frames  [ 0 ]  ~  [ 100 ]
                   ↑開始フレーム ↑終了フレーム
```

### ステップ 5: [Run Expression] をクリック

処理が実行されます。

```
✓ 成功時: ボタンが緑色に "Executed" と表示
✗ 失敗時: エラーメッセージが表示
```

---

## CEL式エディタ

### CELとは？

**比較的シンプルな計算式言語**。TCLより簡単です。

### 🎁 便利なプリセット

CEL式の入力欄に以下を入力すると、よく使う式に自動展開されます：

```
$loop
  ↓
(frame - 1) % seq_count + 1
（ループ再生）

$reverse
  ↓
seq_count - ((frame - 1) % seq_count)
（逆順再生）

$hold_3frames
  ↓
((frame - 1) / 2) % seq_count + 1
（1フレームを2回表示）

$ping_pong
  ↓
前後に揺らすような再生
```

### 使い方

#### ステップ 1: 式エディタを選択

```
画面下部のドロップダウン: [CEL ▼]
```

#### ステップ 2: 式を入力

```
入力欄: [ CEL expression ... ]
```

- **手動入力**: `(frame - 1) % seq_count + 1`
- **プリセット**: `$loop` と入力 → 式が展開される

#### ステップ 3: フレーム範囲を指定

```
loop over frames  [ 0 ]  ~  [ 100 ]
```

#### ステップ 4: [Run Expression] をクリック

処理が実行されます。

### 📝 CEL式の構文

| 演算子 | 説明 | 例 |
|--------|------|-----|
| `+` | 足す | `frame + 10` |
| `-` | 引く | `frame - 1` |
| `*` | 掛ける | `frame * 2` |
| `/` | 割る | `frame / 2` |
| `%` | 割り算の余り | `frame % 10` |
| `?` `:` | 条件分岐 | `frame > 50 ? 1 : 2` |

---

## キーボードショートカット

### 🎮 フレーム移動

| キー | 動作 |
|------|------|
| `→` | 次のフレーム |
| `←` | 前のフレーム |
| `Shift + →` | 10フレーム進む |
| `Shift + ←` | 10フレーム戻る |

### ⌨️ フレームマッピング

| キー | 動作 |
|------|------|
| `0～9` | フレーム番号を入力（1桁ずつ） |
| `Enter` | マッピングを確定 |
| `Delete` | 入力中の数字をクリア |

### ⏯️ 再生制御

| ボタン | 機能 |
|--------|------|
| `[Play]` | シーケンスを再生開始 |
| `[Pause]` | 一時停止 |
| `[Back]` | 最初のフレームに戻す |

---

## メモリ管理

### 📊 バッファとは？

再生を高速化するために、使用するフレーム画像をメモリ（RAM）に事前に読み込むこと。

### [Release Buff] ボタン

```
[Release Buff (45%)]
           ↑
      現在のメモリ使用率
```

メモリ使用率が高い場合、クリックしてメモリを解放します。

```
効果:
- RAM 使用率が低下
- 他のアプリの動作が改善
- 再生時に再度バッファが生成される
```

### 💡 メモリ管理のコツ

- **再生前**: バッファが自動で生成されます
- **再生終了後**: 必要に応じて解放
- **大規模シーケンス**: 定期的に解放してメモリリークを防ぐ

---

# サポート

## よくある質問

### Q1: シーケンス画像が認識されません

**A:** 以下を確認してください：

1. ✅ ファイル名に連続した数字が含まれているか
   ```
   OK: frame_001.png, frame_002.png ...
   NG: frame_a.png, frame_b.png
   ```

2. ✅ すべての画像が同じフォルダにあるか
   ```
   OK: /images/frame_001.png, /images/frame_002.png
   NG: /images/frame_001.png, /other/frame_002.png
   ```

3. ✅ ファイル形式が PNG か
   ```
   推奨: PNG
   代替: JPG, BMP
   ```

---

### Q2: フレームマッピングがうまくいきません

**A:** 以下を確認してください：

1. ✅ 目的のフレーム番号が実際に存在するか
   ```
   例: フレーム200を指定した場合
   → 200.png は存在するか？
   ```

2. ✅ 数字を正しく入力したか
   ```
   例: フレーム 42 を指定する場合
   → 4 → 2 と順に入力 → Enter
   ```

3. ✅ Image Index が目的の番号を表示しているか
   ```
   確認: 画面左上の "Image Index: 42" が期待値か？
   ```

---

### Q3: ビデオレンダリングが失敗します

**A:** 以下を確認してください：

1. ✅ 出力先のフォルダが存在し、書き込み権限があるか
   ```
   [Browse] で有効なフォルダを選択
   ```

2. ✅ ビデオ名に特殊文字が含まれていないか
   ```
   OK: my_video, myVideo_001
   NG: my/video, my|video
   ```

3. ✅ ディスク容量が十分か
   ```
   出力サイズ = 解像度 × フレーム数 × FPS
   例: 1920×1080 × 30fps × 10秒 = 約 1.5GB
   ```

4. ✅ Codec が環境で対応しているか
   ```
   推奨: H.264（ほぼ全ての環境で対応）
   ```

---

### Q4: 式エディタのエラーが出ます

**A:** 以下を確認してください：

**TCL の場合:**

1. ✅ 括弧 `{}` が対応しているか
   ```
   OK: expr { ($frame - 1) % $seq_count + 1 }
   NG: expr ( $frame - 1 ) % $seq_count + 1
   ```

2. ✅ 変数名が正しいか
   ```
   OK: $frame, $seq_count
   NG: $Frame, $seqCount
   ```

**CEL の場合:**

1. ✅ 式が数学的に正しいか
   ```
   OK: (frame - 1) % seq_count + 1
   NG: (frame - 1) % 0  ← ゼロで割っている！
   ```

---

### Q5: 処理が遅い・重い

**A:** 以下を試してください：

1. **メモリを解放**
   ```
   [Release Buff] をクリック
   ```

2. **解像度を下げる**
   ```
   レンダリング設定: 1920×1080 → 1280×720
   ```

3. **FPS を下げる**
   ```
   24 fps → 12 fps
   ```

4. **フレーム範囲を絞る**
   ```
   0～100 → 0～50 で途中まで処理
   ```

---

## トラブルシューティング

### 🔴 エラー: "mata_filename is None"

**原因**: シーケンス画像が読み込まれていない

**対策**:
1. [Read Sequence] をクリック
2. シーケンスの最初の画像を選択
3. もう一度操作を試す

---

### 🔴 エラー: "saving_path is None"

**原因**: プロジェクトが保存されていない

**対策**:
1. [Save] をクリック
2. 保存先を指定
3. もう一度操作を試す

---

### 🔴 画像が黒く表示される

**原因**: テクスチャが正しく読み込まれていない

**対策**:
1. [Release Buff] をクリック
2. フレームを1つ進める
3. フレームを戻す

---

### 🔴 参照ビデオが再生されない

**原因**: ビデオ形式が非対応

**対策**:
1. MP4 形式に変換
   ```
   ffmpeg -i video.avi -c:v h264 video.mp4
   ```
2. [Read Ref] で再度選択

---

### 🔴 メモリ不足でクラッシュ

**原因**: 大規模シーケンスでバッファが満杯

**対策**:
1. [Release Buff] で定期的に解放
2. シーケンスを分割して処理
3. 解像度を下げて処理

---

### 🔴 キーボード入力が反応しない

**原因**: フォーカスがテキスト入力欄にある

**対策**:
1. 画像表示エリアをクリック
2. キーボード操作を試す

---

## 📞 その他の問題

### バグ報告・機能要望

以下の情報を記録して、開発者に報告してください：

```
【報告内容】
1. 問題が発生した時の操作手順
2. エラーメッセージ（スクリーンショットなど）
3. 環境情報
   - OS: Windows / Mac / Linux
   - Python バージョン
   - メモリ容量
   - GPU 情報（OpenGL対応か）
4. シーケンス情報
   - 画像数
   - 解像度
   - ファイル形式
```

---

## 📚 参考資料

### 🔗 外部リンク

- **PySide6 ドキュメント**: https://doc.qt.io/qtforpython/
- **OpenGL 入門**: https://www.khronos.org/opengl/
- **TCL 言語**: https://www.tcl.tk/

### 💾 ファイル形式

- **シーケンス**: PNG, JPG, BMP
- **ビデオ**: MP4 (H.264 推奨)
- **プロジェクト**: *.sdproj

---

## 🎓 まとめ

### 基本の流れ

```
1. [Read Sequence] → シーケンス読み込み
2. [Read Ref] → 参照ビデオ設定（オプション）
3. フレーム移動 → 目的のフレームに移動
4. 数字入力 + Enter → フレームマッピング
5. [Render] → ビデオ出力
```

### 高度な使い方

```
- TCL式で複雑な自動処理
- CELプリセットで簡単な処理
- キーボードショートカットで効率化
- メモリ管理で安定動作
```

### 💡 作業効率化のコツ

- **プロジェクトを保存** して後で復元
- **式エディタを活用** して一括処理
- **メモリを定期的に解放** して安定性向上
- **キーボード操作** でマウス移動を削減

---

**Happy Frame Mapping! 🎬**

---

*この取扱説明書は Shell Delta v1.0 向けです。*
*アプリケーションのアップデートにより、内容が変わる可能性があります。*
